use console_engine::KeyCode;

use super::axis::Axis;

pub struct Bindings {
}
impl Bindings {
    pub fn new() -> Self {
        Self { }
    }

    pub fn get_axial_bindings(&self, axis: Axis) {

    }

    pub fn bind_key_to_axis(&self, axis: Axis, positive_key: KeyCode, negative_key: KeyCode) {

    }
}